# Granted permissions

The user moves blocks here from `PERMISSIONS_PENDING.md` to grant them. On the next run, the agent executes them and moves the blocks to `## Consumed` below.

## Active grants

_(empty)_

## Consumed

_(agent appends here after acting on a grant; keep last ~20 entries for audit)_
