# Inbox

Drop tasks or notes for the agent here as individual `.md` files. The agent treats inbox items as the highest priority work category.

Suggested format: one file per task, name it descriptively (e.g. `2026-06-08-summarise-vault.md`). The agent will delete or move the file once it's handled.
